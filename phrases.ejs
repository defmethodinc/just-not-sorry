# List of Warning Phrases
<table>
  <thead>
  <tr>
    <th>phrase</th>
    <th>message</th>
  </tr>
  </thead>
  <tbody><% phrases.forEach((phrase) => { %>
  <tr>
    <th>
      <ul><% phrase.displayLabel.forEach((label) => { %>
        <li>"<%= label %>"</li><% }); %>
      </ul>
    </th>
    <td>
      <%= phrase.message %> <a href="<%= phrase.source %>" rel="noopener noreferrer" target="_blank">source</a>
    </td>
  </tr><% }); %>
  </tbody>
</table>
